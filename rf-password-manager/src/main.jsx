import React from 'react';
import ReactDOM from 'react-dom/client';
import RFPasswordManagerPopup from './RFPasswordManagerPopup.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RFPasswordManagerPopup />
  </React.StrictMode>
);
